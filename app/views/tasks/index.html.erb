<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
  <h1>タスク一覧</h1>
  <div class="row">
      <div class="col-sm-8">
        <%= render "tasks/tasks", tasks: @task %>
      </div>
      <div>
        <%= link_to "新規作成ページ", new_task_path %>
      </div>
  </div>
<% end %>
